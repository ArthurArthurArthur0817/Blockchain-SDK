import { Account, Chain, PublicClient, Transport, WalletClient } from 'viem';
export declare function createWalletClientFromUrl(rpcUrl: string, chainId: number, account: Account): WalletClient<Transport, Chain>;
export declare function createPublicClientFromUrl(rpcUrl: string, chainId: number, pollingInterval?: number): PublicClient;
export declare function getChainIdFromUrl(rpcUrl: string): Promise<number>;
export declare function createPublicClientWithChainIdFromUrl(rpcUrl: string): Promise<PublicClient>;
export declare function getClientsForDeployment(url: string): Promise<{
    publicClient: PublicClient;
    walletClient: WalletClient<Transport, Chain>;
}>;
