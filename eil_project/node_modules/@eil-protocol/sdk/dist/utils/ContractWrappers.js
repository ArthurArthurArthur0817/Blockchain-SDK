import { getContract } from "viem";
import { EntryPointMeta, ICrossChainPaymaster, TestERC20Meta } from "../abitypes/abiTypes.js";
export function getEntryPoint(client, address) {
    return getContract({
        abi: EntryPointMeta.abi,
        address,
        client
    });
}
export function getICrossChainPaymaster(client, address) {
    return getContract({
        abi: ICrossChainPaymaster.abi,
        address,
        client
    });
}
export function getTestErc20Token(client, address) {
    return getContract({
        abi: TestERC20Meta.abi,
        address,
        client
    });
}
//# sourceMappingURL=ContractWrappers.js.map