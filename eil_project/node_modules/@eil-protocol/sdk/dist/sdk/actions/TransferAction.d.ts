import { Address, Call } from 'viem';
import { BaseAction, BatchBuilder, FunctionCall, MultichainToken, RuntimeVar } from '../index.js';
export declare class TransferAction implements BaseAction {
    readonly data: {
        token: MultichainToken;
        recipient: Address;
        amount: bigint | RuntimeVar;
    };
    constructor(data: {
        token: MultichainToken;
        recipient: Address;
        amount: bigint | RuntimeVar;
    });
    encodeCall(batch: BatchBuilder): Promise<Array<Call | FunctionCall>>;
}
