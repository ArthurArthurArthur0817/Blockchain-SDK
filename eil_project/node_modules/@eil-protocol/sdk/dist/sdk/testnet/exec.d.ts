import { Account, Address, Chain, PublicClient, Transport, WalletClient } from 'viem';
export declare function getMasterAccount(client: PublicClient, index?: number): Promise<Address>;
/**
 * Create a WalletClient from a PublicClient for a testnet account
 * query the "master" account of this chain, and create a WalletClient, so we can make transaction on this chain.
 * @param publicClient - The PublicClient to use for the wallet client.
 * @returns A WalletClient instance.
 */
export declare function toWalletClient(publicClient: PublicClient): Promise<WalletClient<Transport, Chain, Account>>;
export declare function fund(publicClient: PublicClient, target: Address, amount?: bigint): Promise<void>;
