import { AddressPerChain, MultichainContract } from './MultichainContract.js';
import { Address } from 'viem';
import { MultichainClient } from './MultichainClient.js';
import { IMultiChainEntity } from './index.js';
export type TotalBalanceOfResult = {
    perChainBalance: Array<{
        chainId: bigint;
        balance: bigint;
    }>;
    totalBalance: bigint;
};
export declare class MultichainToken extends MultichainContract {
    constructor(name: string, client: MultichainClient, deployments: AddressPerChain);
    balanceOf(chainId: bigint, address: Address | IMultiChainEntity): Promise<bigint>;
    symbol(chainId: bigint): Promise<string>;
    decimals(chainId: bigint): Promise<number>;
    allowance(chainId: bigint, owner: IMultiChainEntity, spender: IMultiChainEntity): Promise<bigint>;
    totalBalanceOf(targetAddress: Address | IMultiChainEntity): Promise<TotalBalanceOfResult>;
}
