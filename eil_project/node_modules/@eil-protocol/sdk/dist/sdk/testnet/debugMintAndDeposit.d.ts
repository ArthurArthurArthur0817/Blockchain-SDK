import { Address, PublicClient } from "viem";
import { MultichainToken } from "../index.js";
/**
 * mint tokens, and deposit them for the provider in the paymaster.
 * This method is for forked testnets:  it requires debugSetTokenAmount() which uses hardhat/anvil internal storage APIs
 * @param publicClient
 * @param paymasterAddress
 * @param providerAddress
 * @param mcToken
 * @param amount
 */
export declare function debugMintAndDeposit(publicClient: PublicClient, paymasterAddress: Address, providerAddress: Address, token: MultichainToken | Address, amount: bigint): Promise<void>;
