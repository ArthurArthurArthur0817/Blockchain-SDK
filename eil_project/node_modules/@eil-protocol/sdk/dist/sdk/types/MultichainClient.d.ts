import { Address, PublicClient } from "viem";
import { ChainInfo } from '../config/index.js';
export declare class MultichainClient {
    clients: Map<bigint, PublicClient>;
    static create(chains: ChainInfo[]): MultichainClient;
    constructor(publicClients?: PublicClient[]);
    all(): PublicClient[];
    addClient(client: PublicClient): Promise<void>;
    addClientWithChainId(client: PublicClient, chainId: bigint): void;
    clientOn(chainId: bigint): PublicClient;
    call({ chainId, to, abi, functionName, args, value }: {
        chainId: bigint;
        to: Address;
        abi: readonly unknown[];
        functionName: string;
        args: any[];
        value?: bigint;
    }): Promise<any>;
}
