import { Address, Hex } from 'viem';
import { IBundlerManager } from '../types/IBundlerManager.js';
import { ChainInfo } from '../config/index.js';
import { UserOperation } from '../types/UserOperation.js';
import { IJsonRpcProvider } from '../types/index.js';
export declare class MultichainBundlerManager implements IBundlerManager {
    readonly chainInfos: ChainInfo[];
    constructor(chainInfos: ChainInfo[]);
    bundlers: Map<bigint, IJsonRpcProvider>;
    entryPoints: Map<bigint, Address>;
    addBundler(chainId: bigint, provider: IJsonRpcProvider, entryPointAddress: Address): void;
    verifyConfig(chainId: bigint, entryPoint: Address): Promise<void>;
    sendUserOperation(userOp: UserOperation): Promise<Hex>;
}
