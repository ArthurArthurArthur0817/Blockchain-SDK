import { Call } from 'viem';
import { BaseAction, BatchBuilder, FunctionCall, SdkVoucherRequest } from '../index.js';
/**
 * The internal class defining an action to lock the user deposit for the specified {@link SdkVoucherRequest}.
 */
export declare class VoucherRequestAction implements BaseAction {
    readonly voucherRequest: SdkVoucherRequest;
    private nativeAmount;
    constructor(voucherRequest: SdkVoucherRequest);
    encodeCall(batch: BatchBuilder): Promise<Array<Call | FunctionCall>>;
}
