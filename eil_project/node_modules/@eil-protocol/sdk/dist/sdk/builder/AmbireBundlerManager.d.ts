import { Address, Hex } from 'viem';
import { IBundlerManager, SendRawUserOperationInput } from '../types/IBundlerManager.js';
import { UserOperation } from '../types/UserOperation.js';
import { IJsonRpcProvider } from '../types/index.js';
export declare class AmbireBundlerManager implements IBundlerManager {
    readonly walletProvider: IJsonRpcProvider;
    readonly entryPoints: Map<bigint, Address>;
    constructor(walletProvider: IJsonRpcProvider, entryPoints: Map<bigint, Address>);
    verifyConfig(_: bigint): Promise<void>;
    sendUserOperation(userOp: UserOperation): Promise<Hex>;
    sendRawUserOperation(userOps: SendRawUserOperationInput): Promise<Hex>;
}
