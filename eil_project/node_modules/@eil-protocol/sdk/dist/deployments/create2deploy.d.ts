import { Address, Chain, Hex, Transport, WalletClient } from 'viem';
export declare const createXaddress: Address;
export declare const saltForBytecodeOnly: `0x${string}`;
export declare function deployCreateX(client: WalletClient<Transport, Chain>): Promise<void>;
export declare function getCreate2DeployAddress(metadata: any, ctrParams?: any[], salt?: Hex): Address;
/**
 * deploy contract deterministically, using CreateX's create2.
 * note: works only in test mode. specifically, the given "publicClient" supports sendTransaction
 * @param client
 * @param metadata
 * @param ctrParams
 * @param salt
 */
export declare function create2deploy(client: WalletClient<Transport, Chain>, metadata: any, ctrParams?: any[], salt?: Hex): Promise<Address>;
